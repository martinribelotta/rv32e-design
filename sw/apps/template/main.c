/* template — starting point for a new firmware app.
 * Copy this directory, then build/flash with:  make APP=<name> flash-fw */
#include "soc.h"

int main(void)
{
    /* your firmware here */

    for (;;)
        ;
    return 0;
}
